<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ChatGPTExample" parent="Theme.AppCompat.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Splash  -->
    <style name="Theme.ChatGPTExample.Splash" parent="Theme.SplashScreen.IconBackground">
        <!-- splash icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_layout</item>
        <!-- color -->
        <item name="windowSplashScreenBackground">@color/black</item>
        <!-- splash delay -->
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <!-- splash icon back color
        <item name="windowSplashScreenIconBackgroundColor">@color/white</item> -->
        <!-- splash  -->
        <item name="postSplashScreenTheme">@style/Theme.ChatGPTExample</item>
    </style>

</resources>